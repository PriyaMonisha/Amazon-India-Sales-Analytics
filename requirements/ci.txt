# Minimal packages for CI test runs.
# Excludes: apache-airflow, streamlit, great-expectations, lightgbm, boto3, black.
# These are not imported by any test — skipping them cuts install time from ~15 min to ~3 min.

# Core (subset of base.txt)
pandas==2.1.4
numpy==1.26.4
scipy==1.11.4
pyarrow==15.0.2
sqlalchemy==2.0.42
psycopg2-binary==2.9.9
redis==5.0.1
python-dotenv==1.0.1
PyYAML==6.0.2
requests==2.32.4
tqdm==4.67.1
colorlog==6.9.0
typing_extensions==4.14.1
shap>=0.44.0
slowapi>=0.1.9
filelock>=3.0

# API framework
fastapi==0.115.0
uvicorn==0.30.5
pydantic==2.8.2
pydantic-settings==2.3.4
prometheus-client==0.20.0

# ML — all imported at module level in src/models/
scikit-learn==1.5.2
xgboost==2.0.3
mlxtend==0.23.1
mlflow==2.14.1
optuna==3.5.0
joblib==1.3.2
# prophet and feast are mocked in tests/conftest.py when not installed.
# Installing them in CI causes 10+ minute hangs (cmdstanpy, grpcio C extensions).
# Local dev installs them via requirements/api.txt.

# Monitoring — imported in src/monitoring/drift.py
evidently==0.4.30

# Validation — imported in src/validation/expectations.py
pandera==0.31.1

# Test tooling
pytest==8.3.2
pytest-cov==5.0.0
pytest-mock==3.14.0
httpx==0.27.0
ruff>=0.4.4
